<%= simple_form_for (@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "add a cocktail", class: "btn btn-primary" %>
<% end %>
 <button type=“button” class=“btn btn-success”><%= link_to "retourner à l'accueil", cocktails_path  %>
