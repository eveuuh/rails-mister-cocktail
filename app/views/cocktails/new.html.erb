<%= simple_form_for (@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "add a cocktail", class: "btn btn-ghost" %>
<% end %>
 <button type=“button” class="btn btn-ghost"><%= link_to "retourner à l'accueil", cocktails_path  %>
